function [err] = ResponseError(out, index)
    %UNTITLED Summary of this function goes here
    %   Detailed explanation goes here
    X = out.Data(:,index);
    t = out.Time;
    err = 1/t(end) * 
end